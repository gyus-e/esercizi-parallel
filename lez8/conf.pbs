#!/bin/bash

#PBS -q studenti
#PBS -l nodes=8:ppn=8
#PBS -l mem=1GB
#PBS -N lez8
#PBS -o lez8.out
#PBS -e lez8.err

cd $PBS_O_WORKDIR

sort -u $PBS_NODEFILE > machinefile
NCPU=`wc -l < machinefile`

echo "Il job è in esecuzione sugli $NCPU nodi:"
cat machinefile

MPI_DIR=/usr/lib64/openmpi/1.4-gcc/bin
$MPI_DIR/mpicc -o main main.c
$MPI_DIR/mpiexec -np $NCPU -machinefile machinefile ./main