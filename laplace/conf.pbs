#!/bin/bash

#PBS -q studenti
#PBS -l nodes=8:ppn=8
#PBS -l mem=1GB
#PBS -N laplace
#PBS -o laplace.txt
#PBS -e laplace.err

cd $PBS_O_WORKDIR

sort -u $PBS_NODEFILE > machinefile
NCPU=`wc -l < machinefile`

echo "Il job è in esecuzione sugli $NCPU nodi:"
cat machinefile

make -j
$MPI_DIR/mpiexec -np $NCPU -machinefile machinefile ./laplace.out